A simple game where the user taps a circle twice as fast as possible.

Uses React Native Elements -library along with react-native-tab-view and react-native-dialog.

Highscores are stored with Realm.
